# Production Environment Variables for Frontend
# Since we're deploying frontend-only, we'll set this to a dummy value for now
# You can update this later when your backend is deployed
REACT_APP_API_URL=https://backend-placeholder/api
REACT_APP_ENVIRONMENT=production
REACT_APP_VERSION=1.0.0
GENERATE_SOURCEMAP=false
